<section class="card">
  <a href="/post/{{id}}">
    <header>{{title}}</header>
    <p> {{post_body}}</p>
    <div class="card-body">
      <p>comment_body: {{comment_body}}</p>
      <p>By: {{user_id}}</p>
    </div>
  </a>
</section>